# SDING — 프론트엔드 프로토타입

스딩(SDING) 앱의 **홈 / 전체서비스 / AI플래너 / 커뮤니티 / 마이페이지 5개 탭 전체**를 구현한
프론트엔드 프로토타입입니다. 기획 구체화가 목적이며, AI 응답·이미지 합성·업체 DB·결제·
디스플레이 광고·사진 업로드는 전부 **mock**으로 동작합니다.
(AI플래너 탭 기준 문서: `AI플래너_베타_기획서.md` v2 — 나머지 4탭은 기획서 범위 밖으로,
이번 확장에서 "유니콘 메가앱" 톤에 맞춰 자체 설계했습니다)

## 실행 방법 (로컬 전용, 배포 없음)

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:5199` 접속. 모바일 웹 기준(콘텐츠 최대 너비 430px)이라
브라우저 개발자도구의 모바일 뷰로 보면 가장 자연스럽습니다.

## 화면 구성

### 5개 탭 (하단 네비게이션)

| 경로 | 화면 |
|---|---|
| `/` | **홈** — 히어로 배너 캐러셀, D-day 미니 카드, 빠른 카테고리, AI 플래너 CTA, 인기 업체, 커뮤니티 하이라이트, 디스플레이 광고 |
| `/services` | **전체서비스** — 실시간 검색(카테고리·빠른 액션 필터), AI 합성/체크리스트 바로가기, 14종 카테고리 그리드, 인기 업체 |
| `/planner` | **AI플래너** — D-day, 진행률 도넛, AI 배너, 다음 할 일, 채팅 진입 카드 (구 `/`, 기획서 v2 대시보드) |
| `/community` | **커뮤니티** — 후기/고민상담/자유/정보공유 피드, 카테고리 필터, 네이티브 광고 삽입 |
| `/my` | **마이페이지** — 프로필, 포인트/AI 패스, 준비 현황, 등록 업체, 내 활동, 설정 |

### 하위 화면

| 경로 | 화면 |
|---|---|
| `/checklist` | 웨딩 체크리스트 (31개 항목 · 8개 그룹) + 업체 처리 바텀시트 |
| `/checklist/:itemId/register` | 업체 등록 폼 (상담중/계약완료, 사진 업로드 mock) |
| `/vendors/:category` | 업체 보러가기 (카테고리별 mock 업체 리스트, 14종) |
| `/chat` | AI 웨딩플래너 채팅방 (키워드 매칭 mock 엔진, 온보딩 바로가기 패널) |
| `/vendor-chat` | 업체 채팅 리스트 (등록한 업체만 노출) |
| `/vendor-chat/:itemId` | 업체 1:1 채팅방 (mock 자동응답) |
| `/ai/dress` | AI 드레스 합성 (업체 선택 → 등록 상품 선택 → 합성, mock) |
| `/ai/hall` | AI 웨딩홀 합성 (업체 선택 → 등록 상품 선택 → 합성, mock) |
| `/community/write` | 커뮤니티 글쓰기 (카테고리·사진 첨부 mock) |
| `/community/:postId` | 커뮤니티 게시글 상세 (좋아요·댓글·업체 태그) |

홈·전체서비스·체크리스트·커뮤니티·마이페이지 우하단의 **플로팅 채팅 버튼**으로
AI 플래너 대화 / 업체 채팅을 선택해 진입할 수 있습니다.

## 기술 스택

React 19 + Vite 7 + TypeScript + Tailwind CSS v4 + react-router-dom v7 + lucide-react + framer-motion

## Mock 처리 방식

- **AI 응답**: `src/lib/chatEngine.ts`의 키워드 매칭 스크립트 (자유 대화 아님)
- **이미지 합성**: 사진 → 업체 선택 → 업체 등록 상품(드레스/홀) 선택 → 합성.
  결과는 미리 준비된 정적 이미지에 상품별 CSS 필터를 적용해 변형 노출
- **포인트/구독**: 합성 1회 500P (가입 축하 3,000P 지급), 부족하면
  "스딩 AI 패스" 구독(월 9,900원 표기, mock — 실제 결제 없음) 유도.
  상품 데이터는 `src/data/products.ts`, 비용 상수 `SYNTHESIS_COST`
- **사진 업로드**: `URL.createObjectURL` 로컬 미리보기만 (서버 저장 없음)
- **디스플레이 광고**: `src/data/ads.ts`에 히어로 배너(홈) · 프로모 배너(전체서비스) · 네이티브 피드
  광고(홈/커뮤니티) mock 정의. 전부 가상의 광고주("루나주얼리", "웰스브라이드" 등)이며 실제 광고
  연동은 없습니다. "광고"/"AD" 배지를 항상 표시해 일반 콘텐츠와 구분합니다.
- **커뮤니티**: `src/data/community.ts`에 시드 게시글 8개. 글쓰기·좋아요·댓글은 실제로 상태에 반영되고
  localStorage에 영속화됩니다.
- **상태**: localStorage 영속화 (`sding-beta-state-v1`) — 새로고침해도 체크리스트·채팅·커뮤니티·포인트가
  유지됩니다. 사진 미리보기(blob URL)는 브라우저 세션 한정이라 저장 시 제외됩니다.
  마이페이지 설정 목록의 "베타 데이터 초기화"로 처음 상태로 되돌릴 수 있습니다.

## 참고

- 업체 카테고리는 기획서 v2 기준 **14종** 체계를 사용합니다. 기존 업체 데이터수집 양식(13종)과
  명칭·개수가 다르므로 실제 통합 전에 하나로 맞춰야 합니다.
- 예식일 mock: 2027-01-16, 사용자 mock: 수민님 · 도윤님(예비 배우자)
- 홈/전체서비스/커뮤니티/마이페이지는 기획서 범위 밖 화면이라 실제 서비스 반영 전 기획 검토가 필요합니다.
